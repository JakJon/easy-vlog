# Copy to .env.local and fill in. For production deploys, set in your hosting
# provider's environment variables.
#
# Get a Client ID from Google Cloud Console:
#   APIs & Services → Credentials → Create Credentials → OAuth client ID (Web)
# Required scope: https://www.googleapis.com/auth/photospicker.mediaitems.readonly
VITE_GOOGLE_CLIENT_ID=
