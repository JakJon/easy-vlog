[build]
  command = "npm run build"
  publish = "dist"

[[edge_functions]]
  path = "/api/photos-proxy"
  function = "photos-proxy"

[[headers]]
  for = "/*"
  [headers.values]
    Cross-Origin-Opener-Policy = "same-origin-allow-popups"
    Cross-Origin-Embedder-Policy = "require-corp"
